---
// Empty ThemeSelect override.
// Modulatio is a single-theme site (phosphor amber on deep navy).
// A light variant would break the WOPR-terminal conceit, so the toggle
// is suppressed entirely by rendering nothing here.
---
